// Admins sign in with a shared password only (no email/signup flow).
// Internally this maps to a single, hidden Supabase Auth account.
export const ADMIN_EMAIL = "admin@ai-gala.local";

export const MIN_ADMIN_PASSWORD_LENGTH = 8;
